# Generated by seed.mjs — reproducible from a fixed seed, so not tracked.
# Regenerate with: node seed.mjs
*.csv

# Local scratch: query output, EXPLAIN dumps, psql transcripts.
scratch/
*.out
*.log

# Secrets: connection strings live here locally, never committed.
.env
service/.env

# Node
node_modules/
